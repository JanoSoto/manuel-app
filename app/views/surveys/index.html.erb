<!-- Content Header (Page header) -->
<section class="content-header">
  <h1>
    Encuestas
    <small>Listado de encuestas registradas</small>
  </h1>
  <ol class="breadcrumb">
    <li class="active">Encuestas</li>
  </ol>
</section>

<!-- Main content -->
<section class="content">
  <div class="row">
    <div class="col-xs-12">
      <div class="panel panel-default">
        <div class="panel-body">
          <table class="table">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Descripción</th>
                <th></th>
                <th></th>
                <th></th>
              </tr>
            </thead>

            <tbody>
              <% @surveys.each do |survey| %>
                <tr>
                  <td><%= survey.name %></td>
                  <td><%= survey.description %></td>
                  <td><%= link_to 'Ver', survey %></td>
                  <td><%= link_to 'Editar', edit_survey_path(survey) %></td>
                  <!-- <td><%#= link_to 'Eliminar', survey, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
                </tr>
              <% end %>
            </tbody>
          </table>      
          <%= will_paginate @surveys, param_name: 'page', renderer: BootstrapPagination::Rails %>
          <%= link_to 'Nueva encuesta', new_survey_path, class: 'btn btn-primary btn-flat' %>
        </div>
      </div>
    </div>
  </div>
</section>
<!-- /.content -->
